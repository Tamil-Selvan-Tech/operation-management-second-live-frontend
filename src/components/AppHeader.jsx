export function AppHeader({ title }) {
  return (
    <header className="topbar">
      <div>
        <p className="eyebrow">Project Foundation</p>
        <h1>{title}</h1>
      </div>
    </header>
  )
}
